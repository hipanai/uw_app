# Upwork Auto-Apply Pipeline - Environment Variables
# Copy this file to .env and fill in your values
#
# Required variables are marked with (Required)
# Optional variables are marked with (Optional) and have sensible defaults

# =============================================================================
# ANTHROPIC (Required)
# =============================================================================
# API key for Claude AI - used for pre-filtering, proposal generation, video scripts
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxx

# =============================================================================
# HEYGEN VIDEO GENERATION (Required for video cover letters)
# =============================================================================
# API key for HeyGen video generation
# Get your key at: https://app.heygen.com/settings/api
HEYGEN_API_KEY=xxx

# Your HeyGen avatar ID for video generation
# Find in HeyGen dashboard under Avatars
HEYGEN_AVATAR_ID=xxx

# =============================================================================
# SLACK INTEGRATION (Required for approval workflow)
# =============================================================================
# Slack Bot OAuth token - needs chat:write, chat:write.public scopes
# Create at: https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-xxx

# Slack signing secret for webhook verification
# Found in: Slack App > Basic Information > App Credentials
SLACK_SIGNING_SECRET=xxx

# Channel ID for approval messages (starts with C)
# Find by right-clicking channel > Copy link, ID is at end of URL
SLACK_APPROVAL_CHANNEL=C0123456789

# (Optional) Slack webhook URL for notifications
# Create at: https://api.slack.com/apps > Incoming Webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/xxx/xxx

# =============================================================================
# GOOGLE SHEETS (Required for data storage)
# =============================================================================
# Main pipeline tracking sheet ID
# From URL: https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit
UPWORK_PIPELINE_SHEET_ID=xxx

# Deduplication tracking sheet ID
UPWORK_PROCESSED_IDS_SHEET_ID=xxx

# =============================================================================
# GOOGLE OAUTH (Required)
# =============================================================================
# Path to Google OAuth credentials file
# Download from: Google Cloud Console > APIs & Services > Credentials
GOOGLE_APPLICATION_CREDENTIALS=config/credentials.json

# (Optional) JSON string of Google token data (for Modal deployment)
# GOOGLE_TOKEN_JSON={"token": "...", "refresh_token": "...", ...}

# =============================================================================
# APIFY (Required for job scraping)
# =============================================================================
# Apify API token for Upwork job scraping
# Get at: https://console.apify.com/account#/integrations
APIFY_API_TOKEN=xxx

# =============================================================================
# PIPELINE CONFIGURATION (Optional - have defaults)
# =============================================================================
# Minimum pre-filter score (0-100) to proceed with full processing
# Jobs below this score are filtered out. Default: 70
PREFILTER_MIN_SCORE=70

# Enable debug logging (set to any non-empty value)
# DEBUG=1

# =============================================================================
# PLAYWRIGHT BROWSER (Optional)
# =============================================================================
# Path to persistent browser profile for Upwork authentication
# Must be pre-logged into Upwork manually
PLAYWRIGHT_USER_DATA_DIR=.browser_profile

# =============================================================================
# GMAIL PUSH NOTIFICATIONS (Optional - for real-time email processing)
# =============================================================================
# Google Cloud project ID for Pub/Sub
GOOGLE_CLOUD_PROJECT=your-project-id

# Pub/Sub topic for Gmail push notifications
# Can be short name (gmail-push) or full path (projects/my-project/topics/gmail-push)
GMAIL_PUSH_TOPIC=gmail-push

# Webhook URL for Gmail push notifications (must be HTTPS)
# This is your Modal webhook endpoint
GMAIL_PUSH_WEBHOOK_URL=https://your-modal-app.modal.run/upwork/gmail-push

# =============================================================================
# OTHER INTEGRATIONS (Optional - for existing scripts)
# =============================================================================
# OpenAI API key (if using OpenAI features)
# OPENAI_API_KEY=xxx

# Google Maps API key (if using location features)
# GOOGLE_MAPS_API_KEY=xxx

# PandaDoc API key (if using document generation)
# PANDADOC_API_KEY=xxx

# Instantly API key (if using email features)
# INSTANTLY_API_KEY=xxx

# AnyMailFinder API key (if using email enrichment)
# ANYMAILFINDER_API_KEY=xxx
